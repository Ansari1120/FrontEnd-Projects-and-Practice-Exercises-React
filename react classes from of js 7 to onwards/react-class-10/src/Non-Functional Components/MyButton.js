import React from 'react'
import { Button} from "@mui/material";

export default function MyButton(props) {
    const {variant,color,variant,onClick} = props;
  return (
    <div><Button>
        
        
         </Button> </div>
  )
}
